[phases.setup]
nixPkgs = ["...", "python314", "postgresql", "python314Packages.pip"]

[phases.build]
cmds = [
    "python -m pip install --upgrade pip",
    "python -m pip install -r requirements.txt"
]

[start]
cmd = "gunicorn backend.wsgi:application --bind 0.0.0.0:$PORT"
